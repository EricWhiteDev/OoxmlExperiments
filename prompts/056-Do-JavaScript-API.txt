Below the button "Swap Every Other Para", in the next row, add a button, "Swap Using JavaScript".  When user clicks,

For the first 40 rows, use the JavaScript API to swap every other paragraph.  For example, paragraph 2 becomes paragraph 1, and paragraph 1 becomes paragraph 2.  Then paragraph 3 becomes paragraph 4, and paragraph 4 becomes paragraph 3.  Call context sync between each operation.