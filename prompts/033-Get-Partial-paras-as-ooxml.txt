Make a new button in the task pane to the right of setFont.  Name this button ooxmlPartialPara.  When the user clicks this button, select the text starting at half way through the first paragraph, ending at half way through the third paragraph.  Get the selected text as ooxml.

- Use the LtXmlTs library - parse the XML
- For every run in the paragraph
- if there are no run properties (W.rPr), then add the element to the run.
- Then add the W.b element to the run properties, making the run be bold.
After making all the changes to the Ooxml, then serialize using toString, and put the Ooxml back.
